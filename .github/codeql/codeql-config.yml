name: "Custom CodeQL config"
# Using default query suite for balanced security analysis
# queries:
#   - uses: security-extended
#   - uses: security-and-quality
query-filters:
  - exclude:
      id: py/cyclic-import
